// Background Service Worker
chrome.runtime.onInstalled.addListener(() => {
  console.log('YouTube Link Extractor installed.');
});

// We can add keyboard shortcuts or context menu items here in the future
